export interface IContact {
  name: string;
  email: string;
  phone: string;
}

export interface IMetadata {
  title: string;
  description: string;
  keywords: string;
}

export interface LogoProps {
  className: string;
}
