import { notFound } from "next/navigation";

/**
 * About route is temporarily disabled (returns 404).
 * Original implementation lives in `./_archived/original-page.tsx` — copy it
 * back here to re-enable `/about`.
 */
export default function AboutPage() {
  notFound();
}
